{
  "name": "dashboard-ipr",
  "version": "1.0.0",
  "description": "Dashboard Directivo Inversiones IPR",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required on Vercel'",
    "compile": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "dependencies": {
    "tailwindcss": "^3.4.1"
  }
}
